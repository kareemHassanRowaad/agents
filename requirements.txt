# Forcing a compatible sqlite3 version to be used by chromadb
pysqlite3-binary

# Pinning crewai and chromadb to versions that work together
# and are known to be stable on cloud platforms.
crewai==0.121.1
crewai-tools==0.1.6
chromadb==0.4.22
tiktoken==0.5.2
# Standard app dependencies
streamlit
python-dotenv
